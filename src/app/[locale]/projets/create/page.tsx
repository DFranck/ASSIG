import { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Create Project',
};
const pageProjectsCreate = () => {
  return <div>ICI formulaire de création avec import CSV</div>;
};

export default pageProjectsCreate;
